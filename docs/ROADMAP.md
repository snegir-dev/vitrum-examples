# Vitrum examples roadmap

- [x] Preserve the dependency-free M1 Node offscreen prototype.
- [x] Make its Engine checkout explicit through `VITRUM_ENGINE_DIR`.
- [ ] Replace the prototype helper with `@vitrum/node` after M5.
- [ ] Add vanilla HTML/JS and framework-neutral bundled widget fixtures after
  the Web Profile runner exists.
- [ ] Add .NET embedding examples after the stable managed SDK ships.
- [ ] Add package templates only after `.vitrum` schema validation is stable.
